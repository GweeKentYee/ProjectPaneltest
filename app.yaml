runtime: php
env: flex

runtime_config:
  document_root: public

# Ensure we skip ".env", which is only for local development
skip_files:
  - .env

env_variables:
  # Put production environment variables here.
  APP_LOG: errorlog
  APP_DEBUG: true
  APP_KEY: base64:3EOFvWjr727S0mFtltBYfjtwnRZplJISSLNA9spSaOM=
  STORAGE_DIR: /tmp
  CACHE_DRIVER: database
  SESSION_DRIVER: database
  DB_CONNECTION: mysql
  DB_HOST: 127.0.0.1
  DB_PORT: 3306
  DB_DATABASE: projectpanel
  DB_USERNAME: admin
  DB_PASSWORD: 123456
  DB_SOCKET: "/cloudsql/mimetic-retina-310809:asia-southeast1:testserver-panel"

beta_settings:
    cloud_sql_instances: "mimetic-retina-310809:asia-southeast1:testserver-panel"
